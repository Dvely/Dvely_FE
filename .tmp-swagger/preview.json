{
  "openapi": "3.1.0",
  "info": {
    "title": "Qeploy API",
    "description": "Qeploy backend API — natural-language driven web app generation, GitHub Pages/BYOC deployment, domain binding, and cloud infrastructure operation.\n\n### Common response envelope\nEvery response (except SSE, redirects, GitHub webhook callback, and the preview proxy) is wrapped as:\n```json\n{ \"status\": 200, \"code\": \"SUCCESS\", \"message\": \"...\", \"data\": { } }\n```\n`data` is omitted (not `null`) on responses with no body (e.g. 204). Frontend clients should unwrap `data` after checking `response.ok`; see `docs/FRONTEND_API_GUIDE.md` for the reference `http.ts` implementation.\n\n### Error codes\nErrors use the same envelope with `data` omitted. `code` is one of: `BAD_REQUEST`(400) · `UNAUTHORIZED`(401) · `INVALID_TOKEN`(401) · `EXPIRED_REFRESH_TOKEN`(401) · `REVOKED_REFRESH_TOKEN`(401) · `FORBIDDEN`(403) · `GITHUB_APP_NOT_INSTALLED`(403) · `NOT_FOUND`(404) · `METHOD_NOT_ALLOWED`(405) · `CONFLICT`(409) · `INTERNAL_SERVER_ERROR`(500). `message` carries the specific, human-readable detail — treat `code` as the stable machine-readable discriminator.\n\n### Authentication\nBearer JWT (see the **JWT TOKEN** security scheme below). Obtain a token via the GitHub OAuth flow (`Auth` group), then send `Authorization: Bearer {accessToken}` on every authenticated request. Access tokens expire in 1 hour; use `POST /api/v1/auth/refresh` (rotation — both tokens are replaced) before that. A small set of endpoints are unauthenticated by design: GitHub OAuth/App URL and callback endpoints, `POST /api/v1/auth/refresh`, and `POST /api/v1/webhook/github` (GitHub-signed, not JWT).\n",
    "version": "0.0.1"
  },
  "servers": [{ "url": "/" }],
  "security": [{ "JWT TOKEN": [] }],
  "tags": [
    {
      "name": "Preview",
      "description": "Agent CODE 작업이 띄운 Docker 프리뷰 컨테이너에 대한 리버스 프록시 및 운영(상태/로그) 조회 API."
    }
  ],
  "paths": {
    "/api/v1/previews/{sessionId}/{accessToken}/**": {
      "get": {
        "tags": ["Preview"],
        "summary": "Preview 컨테이너 리버스 프록시",
        "description": "sessionId/accessToken(URL에 내장된 발급 즉시 랜덤 토큰 — JWT 아님, iframe에서 별도 Authorization 헤더 없이 접근하기 위함)이 유효하면 요청을 해당 Docker 컨테이너로 그대로 프록시합니다. 응답 Content-Type은 프리뷰 앱이 반환하는 값 그대로이며(HTML/JS/CSS/이미지 등), 이 API 자체는 공통 응답 envelope로 감싸지 않습니다(@RawApiResponse). 세션이 없거나 토큰이 일치하지 않으면 404입니다. Swagger UI \"Try it out\"으로 직접 호출하기보다는 taskId 폴링으로 받은 previewUrl을 브라우저에서 여는 용도입니다.",
        "operationId": "proxy",
        "parameters": [
          {
            "name": "sessionId",
            "in": "path",
            "description": "Preview 세션 ID",
            "required": true,
            "schema": { "type": "string" }
          },
          {
            "name": "accessToken",
            "in": "path",
            "description": "세션 발급 시 함께 생성된 1회성 접근 토큰(랜덤 UUID)",
            "required": true,
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "*/*": { "schema": { "type": "string", "format": "byte" } } }
          }
        }
      }
    },
    "/api/v1/previews/{sessionId}/{accessToken}": {
      "get": {
        "tags": ["Preview"],
        "summary": "Preview 컨테이너 리버스 프록시",
        "description": "sessionId/accessToken(URL에 내장된 발급 즉시 랜덤 토큰 — JWT 아님, iframe에서 별도 Authorization 헤더 없이 접근하기 위함)이 유효하면 요청을 해당 Docker 컨테이너로 그대로 프록시합니다. 응답 Content-Type은 프리뷰 앱이 반환하는 값 그대로이며(HTML/JS/CSS/이미지 등), 이 API 자체는 공통 응답 envelope로 감싸지 않습니다(@RawApiResponse). 세션이 없거나 토큰이 일치하지 않으면 404입니다. Swagger UI \"Try it out\"으로 직접 호출하기보다는 taskId 폴링으로 받은 previewUrl을 브라우저에서 여는 용도입니다.",
        "operationId": "proxy_1",
        "parameters": [
          {
            "name": "sessionId",
            "in": "path",
            "description": "Preview 세션 ID",
            "required": true,
            "schema": { "type": "string" }
          },
          {
            "name": "accessToken",
            "in": "path",
            "description": "세션 발급 시 함께 생성된 1회성 접근 토큰(랜덤 UUID)",
            "required": true,
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": { "*/*": { "schema": { "type": "string", "format": "byte" } } }
          }
        }
      }
    },
    "/api/v1/preview-sessions/{sessionId}/status": {
      "get": {
        "tags": ["Preview"],
        "summary": "Preview 컨테이너 상태 조회",
        "description": "세션의 Docker 컨테이너 실행 여부·리소스 사용량을 조회합니다. stats one-shot 샘플링 특성상 CPU 델타 계산에 ~1초가 소요되어 이 API의 p95 지연은 약 1.5초입니다 — FE 폴링 주기는 5초 이상을 권장합니다. stats 조회가 3초를 넘기면 resources만 null로 응답합니다(상태 필드는 정상 반환). 종료된 세션도 조회 가능하며 이 경우 containerRunning=false로 응답합니다.",
        "operationId": "getStatus",
        "parameters": [
          { "name": "sessionId", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "*/*": { "schema": { "$ref": "#/components/schemas/PreviewContainerStatusResponse" } }
            }
          }
        }
      }
    },
    "/api/v1/preview-sessions/{sessionId}/logs": {
      "get": {
        "tags": ["Preview"],
        "summary": "Preview 컨테이너 로그 조회",
        "description": "세션의 Docker 컨테이너 stdout/stderr를 각 줄 타임스탬프가 포함된 단일 텍스트로 반환합니다. 로그는 영속화되지 않으며 컨테이너 제거 시 함께 소멸합니다(다운로드/스트리밍 미지원). tail은 기본 200이며 [1, 2000] 범위를 벗어나면 에러 없이 경계값으로 클램프됩니다. sinceSeconds는 절대 시각이 아닌 '최근 N초' 상대값입니다. 컨테이너가 이미 제거된 세션은 404가 아닌 containerRunning=false, logText=\"\" 200 응답입니다.",
        "operationId": "getLogs",
        "parameters": [
          { "name": "sessionId", "in": "path", "required": true, "schema": { "type": "string" } },
          {
            "name": "tail",
            "in": "query",
            "required": false,
            "schema": { "type": "integer", "format": "int32" }
          },
          {
            "name": "sinceSeconds",
            "in": "query",
            "required": false,
            "schema": { "type": "integer", "format": "int32" }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "*/*": { "schema": { "$ref": "#/components/schemas/PreviewContainerLogsResponse" } }
            }
          }
        }
      }
    },
    "/api/v1/preview-sessions/{sessionId}": {
      "delete": {
        "tags": ["Preview"],
        "summary": "Preview session 종료",
        "description": "PreviewSession을 즉시 만료 처리하고 연결된 Docker 컨테이너를 종료합니다. 이미 종료/만료된 세션이거나 다른 유저 소유 세션이면 404를 반환합니다.",
        "operationId": "close",
        "parameters": [
          { "name": "sessionId", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "OK" } }
      }
    }
  },
  "components": {
    "schemas": {
      "PreviewContainerStatusResponse": {
        "type": "object",
        "description": "Preview 컨테이너 상태 응답",
        "properties": {
          "sessionId": { "type": "string", "description": "Preview 세션 ID" },
          "projectId": { "type": "integer", "format": "int64", "description": "프로젝트 ID" },
          "taskId": { "type": "string", "description": "연결된 agent 태스크 ID" },
          "sessionStatus": {
            "type": "string",
            "description": "세션 상태: ACTIVE | CLOSED | EXPIRED"
          },
          "containerRunning": { "type": "boolean", "description": "Docker 컨테이너 실행 여부" },
          "oomKilled": {
            "type": "boolean",
            "description": "OOM(메모리 초과) kill 여부. 컨테이너가 없거나 확인 불가하면 null"
          },
          "exitCode": {
            "type": "integer",
            "format": "int64",
            "description": "컨테이너 종료 코드. 실행 중이거나 확인 불가하면 null"
          },
          "startedAt": {
            "type": "string",
            "format": "date-time",
            "description": "컨테이너 기동 시각. 없으면 null"
          },
          "expiresAt": {
            "type": "string",
            "format": "date-time",
            "description": "세션 만료 예정 시각"
          },
          "resources": {
            "$ref": "#/components/schemas/ResourceUsageResponse",
            "description": "리소스 사용량. 미실행이거나 stats 조회 실패(3초 초과 포함) 시 null"
          }
        }
      },
      "ResourceUsageResponse": {
        "type": "object",
        "description": "컨테이너 리소스 사용량",
        "properties": {
          "memoryUsageBytes": {
            "type": "integer",
            "format": "int64",
            "description": "메모리 사용량(byte)"
          },
          "memoryLimitBytes": {
            "type": "integer",
            "format": "int64",
            "description": "메모리 제한(byte). 격리 정책값(1 GiB)과 동일"
          },
          "memoryUsagePercent": {
            "type": "number",
            "format": "double",
            "description": "메모리 사용률(%), 소수 1자리"
          },
          "cpuPercent": {
            "type": "number",
            "format": "double",
            "description": "CPU 사용률(%), 소수 1자리. cpu/precpu 델타 기반"
          }
        }
      },
      "PreviewContainerLogsResponse": {
        "type": "object",
        "description": "Preview 컨테이너 로그 응답",
        "properties": {
          "sessionId": { "type": "string", "description": "Preview 세션 ID" },
          "containerRunning": { "type": "boolean", "description": "Docker 컨테이너 실행 여부" },
          "logText": {
            "type": "string",
            "description": "stdout+stderr 병합 로그 텍스트. 각 줄은 Docker 타임스탬프로 시작. 컨테이너가 이미 제거된 세션은 빈 문자열"
          }
        }
      }
    },
    "securitySchemes": {
      "JWT TOKEN": {
        "type": "http",
        "name": "JWT TOKEN",
        "scheme": "Bearer",
        "bearerFormat": "JWT"
      }
    }
  }
}
