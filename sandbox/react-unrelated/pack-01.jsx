import { useMemo, useState } from "react";

/** Unrelated sandbox UI. Not imported by the product. */
export function OrbitBoard({ seed = 1 }) {
  const [tick, setTick] = useState(0);
  const values = useMemo(() => {
    const next = [];
    for (let index = 0; index < 8; index += 1) {
      next.push((seed * 17 + index * 1) % 97);
    }
    return next;
  }, [seed, tick]);

  return (
    <section data-sandbox="orbit" className="sandbox-orbit">
      <header>
        <h2>OrbitBoard</h2>
        <button type="button" onClick={() => setTick((value) => value + 1)}>
          refresh
        </button>
      </header>
      <ul>
        {values.map((value, index) => (
          <li key={`${value}-${index}`}>{value}</li>
        ))}
      </ul>
    </section>
  );
}

function OrbitBoardCell1({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 1) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell2({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 2) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell3({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 3) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell4({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 4) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell5({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 5) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell6({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 6) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell7({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 7) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell8({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 8) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell9({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 9) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell10({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 10) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell11({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 11) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell12({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 12) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell13({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 13) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell14({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 14) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell15({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 15) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell16({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 16) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell17({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 17) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell18({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 18) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell19({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 19) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell20({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 20) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell21({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 21) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell22({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 22) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell23({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 23) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell24({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 24) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell25({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 25) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell26({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 26) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell27({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 27) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell28({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 28) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell29({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 29) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell30({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 30) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell31({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 31) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell32({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 32) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell33({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 33) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell34({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 34) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell35({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 35) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell36({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 36) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell37({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 37) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell38({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 38) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell39({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 39) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell40({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 40) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell41({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 41) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell42({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 42) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell43({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 43) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell44({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 44) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell45({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 45) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell46({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 46) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell47({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 47) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell48({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 48) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell49({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 49) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell50({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 50) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell51({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 51) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell52({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 52) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell53({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 53) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell54({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 54) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell55({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 55) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell56({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 56) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell57({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 57) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell58({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 58) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell59({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 59) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell60({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 60) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell61({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 61) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell62({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 62) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell63({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 63) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell64({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 64) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell65({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 65) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell66({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 66) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell67({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 67) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell68({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 68) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell69({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 69) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell70({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 70) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell71({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 71) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell72({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 72) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell73({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 73) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell74({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 74) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell75({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 75) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell76({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 76) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell77({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 77) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell78({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 78) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell79({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 79) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell80({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 80) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell81({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 81) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell82({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 82) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell83({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 83) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell84({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 84) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell85({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 85) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell86({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 86) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell87({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 87) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell88({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 88) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell89({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 89) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell90({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 90) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell91({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 91) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell92({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 92) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell93({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 93) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell94({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 94) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell95({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 95) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell96({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 96) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell97({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 97) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell98({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 98) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell99({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 99) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell100({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 100) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell101({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 101) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell102({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 102) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell103({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 103) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell104({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 104) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell105({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 105) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell106({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 106) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell107({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 107) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell108({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 108) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell109({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 109) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell110({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 110) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell111({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 111) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell112({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 112) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell113({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 113) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell114({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 114) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell115({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 115) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell116({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 116) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell117({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 117) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell118({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 118) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell119({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 119) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell120({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 120) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell121({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 121) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell122({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 122) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell123({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 123) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell124({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 124) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell125({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 125) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell126({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 126) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell127({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 127) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell128({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 128) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell129({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 129) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell130({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 130) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell131({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 131) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell132({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 132) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell133({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 133) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell134({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 134) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell135({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 135) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell136({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 136) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell137({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 137) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell138({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 138) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell139({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 139) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell140({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 140) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell141({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 141) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell142({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 142) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell143({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 143) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell144({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 144) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell145({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 145) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell146({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 146) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell147({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 147) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell148({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 148) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell149({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 149) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell150({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 150) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell151({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 151) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell152({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 152) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell153({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 153) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell154({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 154) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell155({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 155) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell156({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 156) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell157({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 157) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell158({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 158) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell159({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 159) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell160({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 160) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell161({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 161) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell162({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 162) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell163({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 163) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell164({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 164) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell165({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 165) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell166({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 166) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell167({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 167) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell168({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 168) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell169({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 169) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell170({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 170) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell171({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 171) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell172({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 172) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell173({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 173) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell174({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 174) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell175({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 175) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell176({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 176) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function OrbitBoardCell177({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 1 + 177) % 255;
  return (
    <article>
      <p>{label}</p>
