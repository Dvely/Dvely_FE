import { useMemo, useState } from "react";

/** Unrelated sandbox UI. Not imported by the product. */
export function PathfinderGrid({ seed = 8 }) {
  const [tick, setTick] = useState(0);
  const values = useMemo(() => {
    const next = [];
    for (let index = 0; index < 8; index += 1) {
      next.push((seed * 17 + index * 8) % 97);
    }
    return next;
  }, [seed, tick]);

  return (
    <section data-sandbox="path" className="sandbox-path">
      <header>
        <h2>PathfinderGrid</h2>
        <button type="button" onClick={() => setTick((value) => value + 1)}>
          refresh
        </button>
      </header>
      <ul>
        {values.map((value, index) => (
          <li key={`${value}-${index}`}>{value}</li>
        ))}
      </ul>
    </section>
  );
}

function PathfinderGridCell1({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 1) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell2({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 2) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell3({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 3) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell4({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 4) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell5({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 5) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell6({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 6) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell7({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 7) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell8({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 8) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell9({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 9) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell10({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 10) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell11({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 11) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell12({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 12) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell13({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 13) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell14({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 14) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell15({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 15) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell16({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 16) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell17({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 17) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell18({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 18) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell19({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 19) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell20({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 20) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell21({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 21) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell22({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 22) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell23({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 23) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell24({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 24) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell25({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 25) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell26({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 26) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell27({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 27) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell28({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 28) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell29({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 29) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell30({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 30) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell31({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 31) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell32({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 32) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell33({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 33) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell34({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 34) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell35({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 35) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell36({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 36) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell37({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 37) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell38({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 38) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell39({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 39) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell40({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 40) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell41({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 41) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell42({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 42) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell43({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 43) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell44({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 44) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell45({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 45) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell46({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 46) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell47({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 47) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell48({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 48) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell49({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 49) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell50({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 50) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell51({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 51) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell52({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 52) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell53({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 53) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell54({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 54) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell55({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 55) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell56({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 56) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell57({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 57) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell58({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 58) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell59({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 59) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell60({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 60) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell61({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 61) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell62({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 62) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell63({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 63) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell64({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 64) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell65({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 65) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell66({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 66) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell67({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 67) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell68({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 68) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell69({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 69) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell70({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 70) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell71({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 71) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell72({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 72) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell73({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 73) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell74({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 74) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell75({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 75) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell76({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 76) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell77({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 77) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell78({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 78) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell79({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 79) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell80({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 80) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell81({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 81) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell82({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 82) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell83({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 83) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell84({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 84) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell85({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 85) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell86({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 86) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell87({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 87) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell88({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 88) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell89({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 89) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell90({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 90) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell91({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 91) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell92({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 92) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell93({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 93) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell94({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 94) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell95({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 95) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell96({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 96) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell97({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 97) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell98({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 98) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell99({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 99) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell100({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 100) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell101({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 101) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell102({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 102) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell103({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 103) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell104({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 104) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell105({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 105) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell106({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 106) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell107({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 107) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell108({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 108) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell109({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 109) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell110({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 110) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell111({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 111) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell112({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 112) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell113({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 113) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell114({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 114) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell115({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 115) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell116({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 116) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell117({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 117) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell118({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 118) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell119({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 119) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell120({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 120) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell121({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 121) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell122({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 122) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell123({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 123) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell124({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 124) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell125({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 125) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell126({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 126) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell127({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 127) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell128({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 128) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell129({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 129) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell130({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 130) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell131({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 131) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell132({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 132) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell133({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 133) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell134({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 134) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell135({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 135) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell136({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 136) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell137({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 137) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell138({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 138) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell139({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 139) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell140({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 140) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell141({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 141) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell142({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 142) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell143({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 143) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell144({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 144) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell145({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 145) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell146({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 146) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell147({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 147) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell148({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 148) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell149({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 149) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell150({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 150) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell151({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 151) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell152({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 152) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell153({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 153) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell154({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 154) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell155({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 155) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell156({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 156) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell157({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 157) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell158({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 158) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell159({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 159) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell160({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 160) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell161({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 161) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell162({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 162) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell163({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 163) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell164({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 164) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell165({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 165) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell166({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 166) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell167({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 167) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell168({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 168) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell169({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 169) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell170({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 170) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell171({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 171) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell172({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 172) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell173({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 173) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell174({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 174) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell175({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 175) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell176({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 176) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PathfinderGridCell177({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 8 + 177) % 255;
  return (
    <article>
      <p>{label}</p>
