import { useMemo, useState } from "react";

/** Unrelated sandbox UI. Not imported by the product. */
export function PacketFrame({ seed = 14 }) {
  const [tick, setTick] = useState(0);
  const values = useMemo(() => {
    const next = [];
    for (let index = 0; index < 8; index += 1) {
      next.push((seed * 17 + index * 14) % 97);
    }
    return next;
  }, [seed, tick]);

  return (
    <section data-sandbox="packet" className="sandbox-packet">
      <header>
        <h2>PacketFrame</h2>
        <button type="button" onClick={() => setTick((value) => value + 1)}>
          refresh
        </button>
      </header>
      <ul>
        {values.map((value, index) => (
          <li key={`${value}-${index}`}>{value}</li>
        ))}
      </ul>
    </section>
  );
}

function PacketFrameCell1({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 1) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell2({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 2) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell3({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 3) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell4({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 4) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell5({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 5) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell6({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 6) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell7({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 7) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell8({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 8) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell9({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 9) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell10({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 10) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell11({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 11) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell12({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 12) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell13({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 13) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell14({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 14) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell15({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 15) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell16({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 16) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell17({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 17) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell18({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 18) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell19({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 19) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell20({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 20) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell21({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 21) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell22({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 22) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell23({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 23) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell24({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 24) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell25({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 25) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell26({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 26) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell27({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 27) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell28({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 28) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell29({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 29) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell30({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 30) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell31({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 31) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell32({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 32) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell33({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 33) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell34({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 34) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell35({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 35) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell36({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 36) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell37({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 37) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell38({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 38) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell39({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 39) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell40({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 40) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell41({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 41) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell42({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 42) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell43({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 43) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell44({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 44) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell45({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 45) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell46({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 46) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell47({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 47) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell48({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 48) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell49({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 49) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell50({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 50) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell51({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 51) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell52({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 52) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell53({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 53) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell54({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 54) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell55({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 55) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell56({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 56) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell57({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 57) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell58({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 58) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell59({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 59) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell60({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 60) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell61({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 61) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell62({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 62) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell63({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 63) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell64({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 64) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell65({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 65) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell66({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 66) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell67({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 67) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell68({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 68) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell69({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 69) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell70({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 70) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell71({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 71) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell72({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 72) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell73({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 73) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell74({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 74) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell75({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 75) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell76({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 76) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell77({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 77) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell78({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 78) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell79({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 79) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell80({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 80) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell81({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 81) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell82({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 82) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell83({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 83) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell84({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 84) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell85({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 85) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell86({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 86) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell87({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 87) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell88({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 88) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell89({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 89) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell90({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 90) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell91({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 91) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell92({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 92) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell93({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 93) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell94({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 94) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell95({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 95) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell96({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 96) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell97({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 97) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell98({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 98) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell99({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 99) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell100({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 100) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell101({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 101) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell102({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 102) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell103({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 103) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell104({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 104) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell105({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 105) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell106({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 106) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell107({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 107) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell108({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 108) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell109({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 109) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell110({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 110) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell111({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 111) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell112({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 112) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell113({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 113) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell114({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 114) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell115({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 115) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell116({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 116) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell117({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 117) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell118({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 118) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell119({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 119) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell120({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 120) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell121({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 121) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell122({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 122) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell123({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 123) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell124({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 124) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell125({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 125) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell126({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 126) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell127({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 127) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell128({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 128) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell129({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 129) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell130({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 130) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell131({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 131) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell132({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 132) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell133({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 133) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell134({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 134) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell135({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 135) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell136({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 136) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell137({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 137) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell138({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 138) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell139({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 139) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell140({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 140) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell141({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 141) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell142({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 142) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell143({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 143) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell144({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 144) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell145({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 145) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell146({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 146) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell147({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 147) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell148({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 148) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell149({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 149) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell150({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 150) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell151({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 151) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell152({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 152) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell153({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 153) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell154({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 154) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell155({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 155) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell156({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 156) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell157({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 157) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell158({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 158) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell159({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 159) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell160({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 160) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell161({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 161) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell162({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 162) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell163({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 163) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell164({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 164) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell165({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 165) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell166({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 166) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell167({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 167) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell168({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 168) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell169({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 169) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell170({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 170) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell171({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 171) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell172({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 172) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell173({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 173) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell174({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 174) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell175({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 175) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell176({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 176) % 255;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function PacketFrameCell177({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 14 + 177) % 255;
  return (
    <article>
      <p>{label}</p>
