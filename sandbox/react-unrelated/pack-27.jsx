import { useMemo, useState } from "react";

/** Unrelated sandbox UI. Not imported by the product. */
export function QuiltPatch({ seed = 27 }) {
  const [tick, setTick] = useState(0);
  const values = useMemo(() => {
    const next = [];
    for (let index = 0; index < 6; index += 1) {
      next.push((seed * 19 + index * 27) % 89);
    }
    return next;
  }, [seed, tick]);

  return (
    <section data-sandbox="quilt">
      <h2>QuiltPatch</h2>
      <button type="button" onClick={() => setTick((value) => value + 1)}>
        refresh
      </button>
      <ul>
        {values.map((value, index) => (
          <li key={`${value}-${index}`}>{value}</li>
        ))}
      </ul>
    </section>
  );
}

function QuiltPatchItem1({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 1) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem2({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 2) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem3({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 3) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem4({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 4) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem5({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 5) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem6({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 6) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem7({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 7) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem8({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 8) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem9({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 9) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem10({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 10) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem11({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 11) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem12({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 12) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem13({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 13) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem14({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 14) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem15({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 15) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem16({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 16) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem17({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 17) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem18({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 18) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem19({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 19) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem20({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 20) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem21({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 21) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem22({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 22) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem23({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 23) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem24({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 24) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem25({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 25) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem26({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 26) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem27({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 27) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem28({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 28) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem29({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 29) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem30({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 30) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem31({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 31) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem32({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 32) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem33({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 33) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem34({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 34) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem35({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 35) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem36({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 36) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem37({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 37) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem38({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 38) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem39({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 39) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem40({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 40) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem41({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 41) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem42({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 42) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem43({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 43) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem44({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 44) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem45({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 45) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem46({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 46) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem47({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 47) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem48({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 48) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem49({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 49) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem50({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 50) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem51({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 51) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem52({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 52) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem53({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 53) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem54({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 54) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem55({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 55) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem56({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 56) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem57({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 57) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem58({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 58) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem59({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 59) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem60({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 60) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem61({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 61) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem62({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 62) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem63({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 63) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem64({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 64) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem65({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 65) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem66({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 66) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem67({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 67) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem68({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 68) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem69({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 69) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem70({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 70) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem71({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 71) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem72({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 72) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem73({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 73) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem74({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 74) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem75({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 75) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem76({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 76) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem77({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 77) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem78({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 78) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem79({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 79) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem80({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 80) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem81({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 81) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem82({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 82) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem83({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 83) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem84({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 84) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem85({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 85) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem86({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 86) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem87({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 87) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem88({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 88) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem89({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 89) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem90({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 90) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem91({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 91) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem92({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 92) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem93({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 93) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem94({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 94) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem95({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 95) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem96({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 96) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem97({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 97) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem98({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 98) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem99({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 99) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem100({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 100) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem101({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 101) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem102({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 102) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem103({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 103) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem104({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 104) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem105({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 105) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem106({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 106) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem107({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 107) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem108({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 108) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem109({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 109) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem110({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 110) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem111({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 111) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem112({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 112) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem113({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 113) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem114({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 114) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem115({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 115) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem116({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 116) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem117({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 117) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem118({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 118) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem119({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 119) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem120({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 120) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

function QuiltPatchItem121({ label, amount }) {
  const [on, setOn] = useState(false);
  const tone = (amount * 27 + 121) % 180;
  return (
    <article>
      <p>{label}</p>
      <p>{tone}</p>
      <button type="button" onClick={() => setOn((value) => !value)}>
        {on ? "on" : "off"}
      </button>
    </article>
  );
}

